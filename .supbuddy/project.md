<!-- AUTO-GENERATED BY SUPBUDDY · DO NOT EDIT (file: .supbuddy/project.md) -->
# Project: map-chat

- **Path:** `/Users/calin/dev/other/wp`
- **ID:** `f9e13085-553a-464e-b693-5b7a6d2642e4`
- **Isolation:** `thin`
- **Loopback IP:** `127.0.0.4` (apps bind this; canonical ports stay free per project)
- **Package manager:** `pnpm`
- **Enabled in Supbuddy:** yes

## Dev servers (thin isolation)

This project has its own loopback IP: `127.0.0.4`. Apps bind it instead of
127.0.0.1, so **every dev server keeps its canonical port** (Next.js `:3000`,
Vite `:5173`) with no cross-project collisions — a busy `127.0.0.1:3000` is
NOT a reason to move this project to another port.

Start dev servers with:

```sh
supbuddy run -- <dev command>     # e.g. supbuddy run -- next dev
```

It injects the right bind flag (`-H 127.0.0.4` for Next, `--host` for
Vite) and ensures the loopback alias exists. Do not pin `-p <other-port>` to
dodge a collision; point mappings at the canonical port instead.

## Apps

| Name | Path | Type | Default port | Mapping(s) |
|------|------|------|--------------|------------|
| map-chat | /Users/calin/dev/other/wp | next |  | — |

## Services

- Kong (service)
- PostgreSQL (service)
- Studio (service)
- Inbucket (service)
- Logflare (service)

## Scripts

| Name | Command | Bookmarked |
|------|---------|------------|
| dev | `supbuddy run -- next dev` | yes |
| build | `next build` | yes |
| start | `next start` | yes |
| lint | `eslint` | no |
| typecheck | `tsc --noEmit` | no |
| test | `vitest run` | yes |
| test:watch | `vitest` | yes |
