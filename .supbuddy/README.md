<!-- AUTO-GENERATED BY SUPBUDDY · DO NOT EDIT (file: .supbuddy/README.md) -->
# Supbuddy project context

This project (**map-chat**, isolation: **thin**)
is managed by Supbuddy. Below are the files this folder contains and how to use them.

## Files in this folder

- `project.md` — project metadata, monorepo apps & services, scripts
- `mappings.md` — domain → port mappings with HTTPS/enabled state
- `services.md` — Supabase / Docker Compose status, ports, connection URLs
- `mcp.md` — Supbuddy MCP server connection info + per-client install snippets
- `do-not.md` — operations to AVOID (manual /etc/resolver or Caddyfile edits, etc.)
- `meta.json` — machine-readable manifest with checksums and timestamps

## How to use

This project's mappings, services, and certs are managed by Supbuddy. Prefer the
Supbuddy MCP tools (`list_mappings`, `create_mapping`, `delete_mapping`, etc.)
over direct filesystem edits.

To verify MCP is connected: call `get_account_info`. If unreachable, see `mcp.md`.

## Last sync

Generated by Supbuddy v0.0.0 at 2026-09-16T18:23:02.764Z.
Run `refresh_project_context` (MCP tool) or click "Sync now" in Supbuddy to refresh.
